export enum ERealtimeRole {
    Candidate = "candidate",
    Guest = "guest",
    Curator = "curator",
    HR = "hr_manager",
    Intern = "intern",
    Mentor = "mentor"
}

export enum EChannel {
    global = "global_chat",
}

export enum ETopic {
    global = "globa_topic",
}

export enum EStatus {
    init = "init"
}